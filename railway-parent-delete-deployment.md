# Parent account cleanup deployment

Latest commit: `2883254` — `Handle parent account cleanup with student deletion`.

Railway observation:

- `d6d9c0a` — `Show forgotten PIN requests in teacher dashboard` is ACTIVE and successful.
- `2883254` — `Handle parent account cleanup with student deletion` is BUILDING and has not become active yet.

The local implementation is ready and tested. The public site should be rechecked after `2883254` becomes ACTIVE.
